<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context="com.aplikasi.aternuscommerce.MainActivity">

    <!-- Banner Image -->

    <!-- Login Button -->

    <ImageView
        android:id="@+id/bannerImageView"
        android:layout_width="352dp"
        android:layout_height="64dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="26dp"
        android:layout_marginEnd="17dp"
        android:layout_marginBottom="16dp"
        android:scaleType="centerCrop"
        android:src="@drawable/logo2" />

    <Button
        android:id="@+id/loginButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Login"
        android:textColor="@color/white"
        android:layout_below="@id/bannerImageView"
        android:layout_marginTop="16dp"
        android:backgroundTint="@color/primary"
    />

    <!-- Other Commerce Components (Placeholder) -->
    <!-- Add your commerce-related components, such as product listings, categories, etc. -->

</RelativeLayout>